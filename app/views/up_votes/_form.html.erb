<%= form_for(up_vote) do |f| %>
  <% if up_vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(up_vote.errors.count, "error") %> prohibited this up_vote from being saved:</h2>

      <ul>
      <% up_vote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :picture_id %>
    <%= f.text_field :picture_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
